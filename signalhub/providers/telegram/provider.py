"""Telegram discovery provider removed — use notifications.TelegramNotificationAdapter."""
from __future__ import annotations

raise ImportError(
    "Telegram is a Notification Adapter, not a discovery Provider. "
    "Import signalhub.notifications.TelegramNotificationAdapter"
)
