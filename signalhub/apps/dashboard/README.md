# Dashboard UI lives in the existing Next.js app at repo `web/`.
# This package is the Core-facing adapter surface for future BFF routes.
#
# Rule: Dashboard calls Core capabilities (same as MCP/API/CLI).
# No business rules in React components beyond presentation.
