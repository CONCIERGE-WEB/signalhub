# SignalHub plugin manifest — Discovery Engine (Dorking)
name: dork_signals
version: 1.0.0
author: "SignalHub Cliente Zero"
description: >
  Discovery Engine: multi-source public discovery via existing Dorking
  (YAML + DDGS + Reddit/HN/RSS). Emits Signal RFC-0001 only. Not Core.
signalhub_version: ">=0.3.0"
contract_version: "1.0.0"
permissions:
  - none
providers:
  - module: dork_signals.provider
    class: DorkSignalsProvider
