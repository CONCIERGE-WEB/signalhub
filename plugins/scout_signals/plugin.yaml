# SignalHub plugin manifest — Cliente Zero (dogfooding the SDK)
name: scout_signals
version: 0.1.0
author: "SignalHub Cliente Zero"
description: >
  First production-path plugin: Scout as SDK extension (not Core).
  Built via create → validate → doctor. Empty until lawful discovery is wired.
signalhub_version: ">=0.3.0"
permissions:
  - none
providers:
  - module: scout_signals.provider
    class: ScoutSignalsProvider
